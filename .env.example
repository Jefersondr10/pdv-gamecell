# Exemplo documental. Para carregar: node --env-file=.env src/server.mjs
HOST=127.0.0.1
PORT=3000
APP_ORIGIN=http://127.0.0.1:3000
DB_PATH=./data/pdv.sqlite
# Valores de desenvolvimento. Não exponha esta configuração diretamente à internet.
# Produção exige HTTPS, Node 24, banco persistente e login Google configurado.
# Configure GOOGLE_CLIENT_ID separadamente para sua própria aplicação Google.
